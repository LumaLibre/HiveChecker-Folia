## HiveChecker w/ Folia

Small port of Folia to HiveChecker. Put your HiveChecker.jar in `sources/` and run
`./gradlew decompileAndApplyPatches`. Your runnable jar will be in `build/libs/`.

HiveChecker is available for free on spigotmc.org.