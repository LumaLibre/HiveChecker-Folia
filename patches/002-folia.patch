diff --git a/__resources__/plugin.yml b/__resources__/plugin.yml
index d90f45c..9d2d789 100644
--- a/__resources__/plugin.yml
+++ b/__resources__/plugin.yml
@@ -4,4 +4,5 @@ version: 3.13.4
 main: dev.hexedhero.hivechecker.HiveChecker
 author: HexedHero
 api-version: 1.13
-website: https://www.spigotmc.org/resources/74913/
\ No newline at end of file
+website: https://www.spigotmc.org/resources/74913/
+folia-supported: true # Luma - folia
\ No newline at end of file
diff --git a/dev/hexedhero/hivechecker/inventories/HiveInventory.java b/dev/hexedhero/hivechecker/inventories/HiveInventory.java
index 9c52318..90858aa 100644
--- a/dev/hexedhero/hivechecker/inventories/HiveInventory.java
+++ b/dev/hexedhero/hivechecker/inventories/HiveInventory.java
@@ -9,6 +9,8 @@ import dev.hexedhero.hivechecker.utils.SkullCreator;
 import java.util.ArrayList;
 import java.util.List;
 import java.util.Random;
+
+import io.papermc.paper.threadedregions.scheduler.ScheduledTask;
 import org.bukkit.Bukkit;
 import org.bukkit.Material;
 import org.bukkit.block.Beehive;
@@ -19,12 +21,11 @@ import org.bukkit.inventory.Inventory;
 import org.bukkit.inventory.ItemStack;
 import org.bukkit.inventory.meta.ItemMeta;
 import org.bukkit.persistence.PersistentDataType;
-import org.bukkit.scheduler.BukkitTask;
 
 public class HiveInventory {
    private static final Random RANDOM = new Random();
    private static final Random SEED = new Random();
-   private BukkitTask task = null;
+   private ScheduledTask task = null;
    private final Player player;
    private final Block block;
    private Beehive hive;
@@ -198,8 +199,8 @@ public class HiveInventory {
             Common.tellConsole(true, "&cPLEASE FIX YOUR \"GUI Live Update Time\" CONFIG TO BE ABOVE 0!");
             timer = 12;
          }
-
-         this.task = Bukkit.getScheduler().runTaskTimer(HiveChecker.getInstance(), () -> {
+         // Luma start - folia
+         this.task = player.getScheduler().runAtFixedRate(HiveChecker.getInstance(), (self) -> {
             if (!this.isHive()) {
                if (this.player.getOpenInventory().getTopInventory().equals(this.inventory)) {
                   this.player.closeInventory();
@@ -208,7 +209,8 @@ public class HiveInventory {
                this.updateBeeAndHoneyAmount();
                this.stockInventory();
             }
-         }, timer, timer);
+         }, null, timer, timer);
+         // Luma end
       }
    }
 
diff --git a/dev/hexedhero/hivechecker/utils/SkullCreator.java b/dev/hexedhero/hivechecker/utils/SkullCreator.java
index 19d4dbf..13201a2 100644
--- a/dev/hexedhero/hivechecker/utils/SkullCreator.java
+++ b/dev/hexedhero/hivechecker/utils/SkullCreator.java
@@ -16,7 +16,7 @@ import java.util.UUID;
 import org.bukkit.Bukkit;
 import org.bukkit.Material;
 import org.bukkit.OfflinePlayer;
-import org.bukkit.SkullType;
+//import org.bukkit.SkullType; // Luma - folia
 import org.bukkit.block.Block;
 import org.bukkit.block.Skull;
 import org.bukkit.inventory.ItemStack;
@@ -110,7 +110,7 @@ public class SkullCreator {
    public static void blockWithUuid(Block block, UUID id) {
       notNull(block, "block");
       notNull(id, "id");
-      setToSkull(block);
+      //setToSkull(block); // Luma - folia
       Skull state = (Skull)block.getState();
       state.setOwningPlayer(Bukkit.getOfflinePlayer(id));
       state.update(false, false);
@@ -138,7 +138,8 @@ public class SkullCreator {
       state.update(false, false);
    }
 
-   private static void setToSkull(Block block) {
+   // Luma start - folia
+   /*private static void setToSkull(Block block) {
       checkLegacy();
 
       try {
@@ -149,7 +150,8 @@ public class SkullCreator {
          state.setSkullType(SkullType.PLAYER);
          state.update(false, false);
       }
-   }
+   }*/
+   // Luma end
 
    private static void notNull(Object o, String name) {
       if (o == null) {
diff --git a/dev/hexedhero/hivechecker/utils/VersionHelper.java b/dev/hexedhero/hivechecker/utils/VersionHelper.java
index 5047c63..4575d4c 100644
--- a/dev/hexedhero/hivechecker/utils/VersionHelper.java
+++ b/dev/hexedhero/hivechecker/utils/VersionHelper.java
@@ -8,7 +8,7 @@ public class VersionHelper {
    private static final int MAJOR_VERSION_NUMBER = Integer.parseInt(parts[0]);
    private static final int MINOR_VERSION_NUMBER = Integer.parseInt(parts[1]);
    private static final int PATCH_VERSION_NUMBER = parts.length < 3 ? 0 : Integer.parseInt(parts[2]);
-   private static final boolean IS_PAPER = checkPaper();
+   //private static final boolean IS_PAPER = checkPaper(); // Luma - folia
 
    private VersionHelper() {
    }
@@ -56,7 +56,7 @@ public class VersionHelper {
    }
 
    public static boolean isPaper() {
-      return IS_PAPER;
+      return true; // Luma - folia
    }
 
    public static boolean isRunningVersionSameOrNewer(String version) {
